# 🌟 World-Class Storyteller Profile Pages
## Design System for Palm Island Community Platform

**Inspired by**: Mukurtu CMS (600+ communities), Ara Irititja (31 Aboriginal communities), Te Hiku Media (Māori innovation)

---

## 🎯 Design Philosophy

**"Every Profile is a Living Archive - Every Person is a Knowledge Keeper"**

Profiles aren't just "user pages" - they're:
- **Living memorials** honoring knowledge keepers
- **Family archives** preserving stories for generations
- **Cultural repositories** protecting traditional knowledge
- **Community connections** showing relationships and kinship

---

## 📐 Profile Page Architecture

### **Layout 1: The Hero Profile** (Default for all storytellers)

```
┌─────────────────────────────────────────────────────────────┐
│ ┌───────────────────────────────────────────────────────┐   │
│ │                                                         │   │
│ │         LARGE PORTRAIT PHOTO (Full-width hero)         │   │
│ │              Professional, Dignified                    │   │
│ │           (From PICC Photo Kiosk Studio)               │   │
│ │                                                         │   │
│ └───────────────────────────────────────────────────────┘   │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 👤 UNCLE FRANK DANIEL LANDERS                           │ │
│ │    Cultural Advisor & Elder                             │ │
│ │    🏝️ Palm Island · 📅 Storyteller since 2024          │ │
│ │    🎵 [AUDIO: "Listen to Uncle Frank introduce himself"]│ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 💬 "My story is our story. When I share, I'm            │ │
│ │     sharing for all my family, all our ancestors."      │ │
│ │                           - Uncle Frank, 2024           │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌──────────────────┐ ┌──────────────────┐ ┌──────────────┐ │
│ │ 📖 Stories: 12   │ │ 🎤 Audio: 8      │ │ 📸 Photos: 45│ │
│ └──────────────────┘ └──────────────────┘ └──────────────┘ │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 📚 THEIR STORIES                                              │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [Story Card 1]                                          │ │
│ │ "Healing Through Culture"                              │ │
│ │ ⏱️ 5 min read · 🎧 Audio available                     │ │
│ │ 🏷️ [TK_Elder_Approved] [Health] [Culture]             │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [Story Card 2]                                          │ │
│ │ "Growing Up on Palm Island"                            │ │
│ │ ⏱️ 8 min read · 📍 Multiple locations                  │ │
│ │ 🏷️ [Community] [History] [Family]                      │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 🌳 FAMILY & CONNECTIONS                                       │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Related Storytellers:                                   │ │
│ │                                                         │ │
│ │ [Photo] Mary Johnson (Sister)                          │ │
│ │ [Photo] Ruby Sibley (Cousin)                           │ │
│ │ [Photo] Uncle Alan (Community Elder)                   │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 🎤 AUDIO BIOGRAPHY                                            │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [▶️ Play Full Interview] (45:23)                        │ │
│ │                                                         │ │
│ │ Chapters:                                               │ │
│ │ • 00:00 - Growing up on Palm Island                    │ │
│ │ • 12:30 - Learning from Elders                         │ │
│ │ • 25:15 - Working with PICC                            │ │
│ │ • 38:40 - Message for young people                     │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 🗺️ PLACES THEY'VE SHARED STORIES ABOUT                      │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [Interactive Map]                                       │ │
│ │                                                         │ │
│ │ 📍 Butler Bay (8 stories)                               │ │
│ │ 📍 Community Center (5 stories)                         │ │
│ │ 📍 Bwgcolman Healing Service (12 stories)              │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 📸 PHOTO GALLERY                                              │
│                                                               │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│ │   [Photo] │ │   [Photo] │ │   [Photo] │ │   [Photo] │   │
│ │  At PICC  │ │ With Fam  │ │At Healing │ │Community  │   │
│ └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│                                                               │
│ [View Full Gallery →]                                        │
│                                                               │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                                               │
│ 🔒 PRIVACY & CULTURAL PROTOCOLS                               │
│                                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ This storyteller has chosen to share their stories     │ │
│ │ with the community. Some stories require elder         │ │
│ │ approval or family permission to view.                 │ │
│ │                                                         │ │
│ │ 🏷️ Cultural Labels Applied:                             │ │
│ │ • [TK Attribution] - Must credit Uncle Frank           │ │
│ │ • [TK Non-Commercial] - Not for commercial use         │ │
│ │ • [TK Family] - Some stories family-only               │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 Design System Specifications

### **Typography**

```typescript
const typography = {
  // Names - Large, Respectful
  storyteller_name: {
    font: 'Inter Variable',
    size: '48px',
    weight: 700,
    lineHeight: 1.2,
    color: '#1a1a1a',
    mobile: '32px'
  },

  // Roles - Clear Identity
  role: {
    font: 'Inter Variable',
    size: '20px',
    weight: 500,
    color: '#4a5568',
    mobile: '16px'
  },

  // Quotes - Prominent
  personal_quote: {
    font: 'Georgia, serif',  // Serif for quotes
    size: '24px',
    weight: 400,
    lineHeight: 1.5,
    style: 'italic',
    color: '#2d3748',
    mobile: '18px'
  },

  // Body Text - Readable
  body: {
    font: 'Inter Variable',
    size: '18px',
    weight: 400,
    lineHeight: 1.6,
    color: '#2d3748',
    mobile: '16px'
  },

  // Accessible minimum (for elders)
  minimum_size: '16px',

  // Maximum line length (readability)
  max_line_length: '65ch'
}
```

### **Color Palette**

```typescript
const colors = {
  // Primary (Ocean - Sovereignty)
  ocean: {
    50: '#e6f6ff',
    100: '#b3e5ff',
    500: '#0077b6',  // Primary blue
    700: '#005a8c',
    900: '#003d5c'
  },

  // Secondary (Earth - Connection)
  earth: {
    50: '#f0f4f0',
    100: '#d4e4d4',
    500: '#2d6a4f',  // Primary green
    700: '#1b4332',
    900: '#0f2419'
  },

  // Cultural (From Palm Island colors)
  cultural: {
    yellow: '#f4c430',  // Sun
    red: '#c1272d',     // Earth
    black: '#1a1a1a',   // Night
    white: '#ffffff'    // Day
  },

  // Neutrals (Accessible)
  neutral: {
    50: '#fafafa',
    100: '#f5f5f5',
    200: '#e5e5e5',
    300: '#d4d4d4',
    400: '#a3a3a3',
    500: '#737373',
    600: '#525252',
    700: '#404040',
    800: '#262626',
    900: '#171717'
  },

  // Semantic (Actions & States)
  semantic: {
    success: '#16a34a',
    warning: '#ea580c',
    error: '#dc2626',
    info: '#0284c7'
  },

  // Cultural Sensitivity Indicators
  access_level: {
    public: '#16a34a',       // Green
    community: '#0284c7',    // Blue
    family: '#ea580c',       // Orange
    restricted: '#dc2626'    // Red
  }
}
```

### **Spacing System**

```typescript
const spacing = {
  xs: '4px',
  sm: '8px',
  md: '16px',
  lg: '24px',
  xl: '32px',
  '2xl': '48px',
  '3xl': '64px',
  '4xl': '96px',

  // Component specific
  card_padding: '24px',
  section_gap: '48px',
  story_card_gap: '16px'
}
```

---

## 🎭 Profile Components

### **1. Hero Section**

```typescript
interface HeroSection {
  portrait: {
    aspect_ratio: '4:5' | '1:1' | '16:9',
    min_resolution: '1200x1500px',
    format: 'webp, jpg fallback',
    alt_text: 'Required, descriptive',

    // From PICC Photo Kiosk
    professional_quality: true,
    studio_lighting: true,
    cultural_context: 'optional background',

    // Accessibility
    face_detection: 'center subject',
    contrast_check: true
  },

  name_badge: {
    full_name: string,
    preferred_name?: string,
    honorific?: 'Uncle' | 'Aunty' | 'Elder',
    role: string,

    // Audio pronunciation
    name_audio: {
      url: string,
      duration: number,
      autoplay: false,
      controls: true,
      label: '"🎵 Listen to name pronunciation"'
    }
  },

  location_time: {
    location: 'Palm Island',
    storyteller_since: Date,
    stories_contributed: number,
    last_active: Date
  },

  audio_intro: {
    url: string,
    duration: '30-60 seconds',
    transcript: string,  // Required for accessibility
    label: '"🎤 Listen to [Name] introduce themselves"'
  }
}
```

**React Component:**

```tsx
export function HeroSection({ storyteller }: { storyteller: Profile }) {
  const [playing, setPlaying] = useState(false)

  return (
    <div className="hero-section">
      {/* Hero Portrait */}
      <div className="relative w-full aspect-[4/5] max-h-[600px]">
        <Image
          src={storyteller.profile_image_url}
          alt={`Portrait of ${storyteller.full_name}, ${storyteller.community_role}`}
          fill
          className="object-cover"
          priority
          sizes="(max-width: 768px) 100vw, 50vw"
        />

        {/* Cultural Labels Overlay */}
        {storyteller.cultural_labels && (
          <div className="absolute bottom-4 left-4 flex gap-2">
            {storyteller.cultural_labels.map(label => (
              <CulturalLabel key={label} type={label} />
            ))}
          </div>
        )}
      </div>

      {/* Name Badge */}
      <div className="p-6 bg-white">
        <div className="flex items-start justify-between">
          <div>
            {storyteller.honorific && (
              <span className="text-lg text-ocean-700 font-semibold">
                {storyteller.honorific}
              </span>
            )}
            <h1 className="text-5xl font-bold text-neutral-900 mt-1">
              {storyteller.full_name}
            </h1>
            {storyteller.preferred_name && (
              <p className="text-xl text-neutral-600 mt-1">
                ({storyteller.preferred_name})
              </p>
            )}
            <p className="text-xl text-neutral-700 mt-2">
              {storyteller.community_role}
            </p>
          </div>

          {/* Audio Pronunciation */}
          {storyteller.name_audio && (
            <button
              onClick={() => setPlaying(!playing)}
              className="flex items-center gap-2 px-4 py-2 bg-ocean-50 hover:bg-ocean-100 rounded-lg transition"
            >
              <span className="text-2xl">{playing ? '⏸️' : '🎵'}</span>
              <span className="text-sm text-ocean-900">
                Hear name
              </span>
            </button>
          )}
        </div>

        {/* Location & Stats */}
        <div className="flex flex-wrap gap-4 mt-4 text-neutral-600">
          <span>🏝️ {storyteller.location}</span>
          <span>📅 Storyteller since {new Date(storyteller.created_at).getFullYear()}</span>
          <span>📖 {storyteller.stories_contributed} stories shared</span>
        </div>

        {/* Audio Introduction */}
        {storyteller.audio_intro && (
          <div className="mt-6">
            <AudioPlayer
              src={storyteller.audio_intro.url}
              label={`Listen to ${storyteller.preferred_name || storyteller.full_name} introduce themselves`}
              transcript={storyteller.audio_intro.transcript}
            />
          </div>
        )}
      </div>
    </div>
  )
}
```

---

### **2. Personal Quote Section**

```tsx
export function PersonalQuote({ quote, attribution, year }: {
  quote: string
  attribution: string
  year?: number
}) {
  return (
    <blockquote className="relative bg-earth-50 p-8 rounded-lg my-8 border-l-4 border-earth-500">
      <div className="text-6xl text-earth-300 absolute top-4 left-4">
        "
      </div>
      <p className="text-2xl italic text-neutral-800 leading-relaxed pl-12">
        {quote}
      </p>
      <footer className="mt-4 text-right text-neutral-600">
        <cite className="not-italic">
          — {attribution}{year && `, ${year}`}
        </cite>
      </footer>
    </blockquote>
  )
}
```

---

### **3. Story Cards Grid**

```tsx
export function StoryCard({ story }: { story: Story }) {
  return (
    <Link
      href={`/stories/${story.id}`}
      className="block bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-md transition group"
    >
      {/* Story Image */}
      {story.featured_image && (
        <div className="relative aspect-[16/9]">
          <Image
            src={story.featured_image}
            alt={story.title}
            fill
            className="object-cover group-hover:scale-105 transition"
          />
        </div>
      )}

      <div className="p-6">
        {/* Title */}
        <h3 className="text-2xl font-semibold text-neutral-900 group-hover:text-ocean-600 transition">
          {story.title}
        </h3>

        {/* Summary */}
        <p className="mt-2 text-neutral-700 line-clamp-2">
          {story.summary}
        </p>

        {/* Metadata */}
        <div className="flex flex-wrap gap-3 mt-4 text-sm text-neutral-600">
          <span>⏱️ {story.reading_time} min read</span>
          {story.has_audio && <span>🎧 Audio available</span>}
          {story.location && <span>📍 {story.location}</span>}
        </div>

        {/* Cultural Labels */}
        <div className="flex flex-wrap gap-2 mt-4">
          {story.tk_labels?.map(label => (
            <span
              key={label}
              className="text-xs px-2 py-1 bg-ocean-50 text-ocean-900 rounded"
            >
              {label}
            </span>
          ))}
          {story.categories?.map(cat => (
            <span
              key={cat}
              className="text-xs px-2 py-1 bg-neutral-100 text-neutral-700 rounded"
            >
              {cat}
            </span>
          ))}
        </div>
      </div>
    </Link>
  )
}
```

---

## 🎤 Audio-First Features

### **Why Audio is Critical**

Based on research from **Te Hiku Media** (Māori language AI) and **Ara Irititja** (Aboriginal oral traditions):

1. **Oral cultures** - Stories meant to be HEARD
2. **Elder accessibility** - Many elders prefer speaking to typing
3. **Language preservation** - Captures pronunciation, cadence, emotion
4. **Low literacy** - No reading required
5. **Authenticity** - Voice carries personality, cultural context

### **Audio Player Component**

```tsx
export function AudioPlayer({
  src,
  label,
  transcript,
  chapters
}: {
  src: string
  label: string
  transcript?: string
  chapters?: Array<{ time: number; title: string }>
}) {
  const [playing, setPlaying] = useState(false)
  const [currentTime, setCurrentTime] = useState(0)
  const [showTranscript, setShowTranscript] = useState(false)

  return (
    <div className="audio-player bg-ocean-50 p-6 rounded-lg">
      {/* Label */}
      <div className="flex items-center gap-3 mb-4">
        <span className="text-3xl">🎤</span>
        <p className="text-lg font-semibold text-ocean-900">{label}</p>
      </div>

      {/* Player Controls */}
      <div className="flex items-center gap-4">
        <button
          onClick={() => setPlaying(!playing)}
          className="w-16 h-16 rounded-full bg-ocean-600 hover:bg-ocean-700 flex items-center justify-center transition"
        >
          <span className="text-3xl text-white">
            {playing ? '⏸️' : '▶️'}
          </span>
        </button>

        {/* Progress Bar */}
        <div className="flex-1">
          <input
            type="range"
            min="0"
            max="100"
            value={currentTime}
            onChange={(e) => setCurrentTime(Number(e.target.value))}
            className="w-full h-2 bg-ocean-200 rounded-lg appearance-none cursor-pointer"
          />
          <div className="flex justify-between text-sm text-neutral-600 mt-1">
            <span>{formatTime(currentTime)}</span>
            <span>{formatTime(100)}</span>
          </div>
        </div>

        {/* Volume */}
        <button className="text-2xl">🔊</button>
      </div>

      {/* Chapters */}
      {chapters && (
        <div className="mt-4 space-y-2">
          <p className="text-sm font-semibold text-neutral-700">Chapters:</p>
          {chapters.map((chapter, i) => (
            <button
              key={i}
              className="block w-full text-left px-3 py-2 rounded hover:bg-ocean-100 transition"
            >
              <span className="text-sm text-ocean-700">{formatTime(chapter.time)}</span>
              <span className="text-sm text-neutral-800 ml-2">{chapter.title}</span>
            </button>
          ))}
        </div>
      )}

      {/* Transcript Toggle */}
      {transcript && (
        <div className="mt-4">
          <button
            onClick={() => setShowTranscript(!showTranscript)}
            className="text-sm text-ocean-700 hover:text-ocean-900 underline"
          >
            {showTranscript ? 'Hide' : 'Show'} transcript
          </button>

          {showTranscript && (
            <div className="mt-3 p-4 bg-white rounded text-neutral-700 text-base leading-relaxed">
              {transcript}
            </div>
          )}
        </div>
      )}
    </div>
  )
}
```

---

## 🗺️ Interactive Maps Component

```tsx
export function StorytellerMap({ places }: {
  places: Array<{
    id: string
    name: string
    coordinates: [number, number]
    story_count: number
    stories: Story[]
  }>
}) {
  const [selectedPlace, setSelectedPlace] = useState<string | null>(null)

  return (
    <div className="storyteller-map">
      <h2 className="text-3xl font-bold mb-6">Places They've Shared Stories About</h2>

      <div className="grid md:grid-cols-2 gap-6">
        {/* Map */}
        <div className="aspect-square bg-ocean-50 rounded-lg overflow-hidden">
          {/* Interactive map goes here - Mapbox or Leaflet */}
          <MapComponent
            places={places}
            onPlaceClick={setSelectedPlace}
          />
        </div>

        {/* Place List */}
        <div className="space-y-3">
          {places.map(place => (
            <button
              key={place.id}
              onClick={() => setSelectedPlace(place.id)}
              className={`
                w-full text-left p-4 rounded-lg transition
                ${selectedPlace === place.id
                  ? 'bg-ocean-100 border-2 border-ocean-500'
                  : 'bg-neutral-50 hover:bg-neutral-100'
                }
              `}
            >
              <div className="flex items-start justify-between">
                <div>
                  <p className="font-semibold text-neutral-900">
                    📍 {place.name}
                  </p>
                  <p className="text-sm text-neutral-600 mt-1">
                    {place.story_count} {place.story_count === 1 ? 'story' : 'stories'}
                  </p>
                </div>
                <span className="text-2xl">→</span>
              </div>
            </button>
          ))}
        </div>
      </div>

      {/* Selected Place Stories */}
      {selectedPlace && (
        <div className="mt-8">
          <h3 className="text-2xl font-semibold mb-4">
            Stories about {places.find(p => p.id === selectedPlace)?.name}
          </h3>
          <div className="grid md:grid-cols-2 gap-4">
            {places
              .find(p => p.id === selectedPlace)
              ?.stories.map(story => (
                <StoryCard key={story.id} story={story} />
              ))}
          </div>
        </div>
      )}
    </div>
  )
}
```

---

## 📸 Photo Gallery Component

```tsx
export function PhotoGallery({ photos }: {
  photos: Array<{
    id: string
    url: string
    caption: string
    date: Date
    location?: string
    people?: Array<{ id: string; name: string }>
  }>
}) {
  const [lightbox, setLightbox] = useState<number | null>(null)

  return (
    <div className="photo-gallery">
      <h2 className="text-3xl font-bold mb-6">📸 Photo Gallery</h2>

      {/* Grid */}
      <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
        {photos.map((photo, index) => (
          <button
            key={photo.id}
            onClick={() => setLightbox(index)}
            className="relative aspect-square bg-neutral-100 rounded-lg overflow-hidden group"
          >
            <Image
              src={photo.url}
              alt={photo.caption}
              fill
              className="object-cover group-hover:scale-110 transition"
            />

            {/* Caption overlay */}
            <div className="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent opacity-0 group-hover:opacity-100 transition">
              <div className="absolute bottom-0 left-0 right-0 p-3">
                <p className="text-white text-sm font-medium line-clamp-2">
                  {photo.caption}
                </p>
              </div>
            </div>
          </button>
        ))}
      </div>

      {/* Lightbox */}
      {lightbox !== null && (
        <PhotoLightbox
          photos={photos}
          currentIndex={lightbox}
          onClose={() => setLightbox(null)}
          onNext={() => setLightbox((lightbox + 1) % photos.length)}
          onPrev={() => setLightbox((lightbox - 1 + photos.length) % photos.length)}
        />
      )}

      <div className="mt-6 text-center">
        <Link
          href="/gallery"
          className="inline-block px-6 py-3 bg-ocean-600 text-white rounded-lg hover:bg-ocean-700 transition"
        >
          View Full Gallery →
        </Link>
      </div>
    </div>
  )
}
```

---

## 🔒 Privacy & Cultural Protocols Section

```tsx
export function CulturalProtocolsNotice({ storyteller }: { storyteller: Profile }) {
  return (
    <div className="cultural-protocols bg-earth-50 p-6 rounded-lg border-l-4 border-earth-500">
      <div className="flex items-start gap-4">
        <span className="text-4xl">🔒</span>
        <div>
          <h3 className="text-xl font-semibold text-neutral-900 mb-3">
            Privacy & Cultural Protocols
          </h3>

          <p className="text-neutral-700 leading-relaxed mb-4">
            {storyteller.preferred_name || storyteller.full_name} has chosen to share their
            stories with the community. Some stories may require elder approval or family
            permission to view, in accordance with cultural protocols.
          </p>

          {/* Applied Labels */}
          {storyteller.tk_labels && storyteller.tk_labels.length > 0 && (
            <div className="space-y-2">
              <p className="font-semibold text-neutral-800">
                🏷️ Cultural Labels Applied:
              </p>
              <ul className="space-y-1">
                {storyteller.tk_labels.map(label => (
                  <li key={label} className="flex items-start gap-2">
                    <span className="text-earth-600">•</span>
                    <span className="text-neutral-700">
                      <strong className="text-earth-900">[{label}]</strong> - {getTKLabelDescription(label)}
                    </span>
                  </li>
                ))}
              </ul>
            </div>
          )}

          {/* Learn More */}
          <div className="mt-4 pt-4 border-t border-earth-200">
            <Link
              href="/cultural-protocols"
              className="text-earth-700 hover:text-earth-900 underline text-sm"
            >
              Learn more about our cultural protocols →
            </Link>
          </div>
        </div>
      </div>
    </div>
  )
}

function getTKLabelDescription(label: string): string {
  const descriptions = {
    'TK Attribution': 'Must credit the knowledge keeper',
    'TK Non-Commercial': 'Not for commercial use',
    'TK Family': 'Some stories restricted to family members',
    'TK Community': 'Community members only',
    'TK Seasonal': 'Viewable only during certain times',
    'TK Elder Approved': 'Elder council has approved this content'
  }
  return descriptions[label] || 'Cultural protocol applied'
}
```

---

## 🎯 Responsive Design

```typescript
// Breakpoints
const breakpoints = {
  mobile: '640px',    // Phones
  tablet: '768px',    // Tablets
  desktop: '1024px',  // Laptops
  wide: '1280px'      // Desktop monitors
}

// Mobile-first approach
const responsiveStyles = `
  /* Mobile (default) */
  .hero-section {
    padding: 16px;
  }

  .storyteller-name {
    font-size: 32px;
  }

  .story-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  /* Tablet */
  @media (min-width: 768px) {
    .hero-section {
      padding: 24px;
    }

    .storyteller-name {
      font-size: 48px;
    }

    .story-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .hero-section {
      padding: 32px;
    }

    .story-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 32px;
    }
  }
`
```

---

## ♿ Accessibility Checklist

```typescript
const accessibilityRequirements = {
  // WCAG AAA Standards
  wcag_level: 'AAA',

  // Visual
  contrast_ratio: '7:1',  // AAA (not just 4.5:1)
  font_size_minimum: '16px',
  touch_target_minimum: '48x48px',

  // Keyboard Navigation
  keyboard_navigable: true,
  focus_indicators: 'visible, high contrast',
  skip_links: true,

  // Screen Readers
  semantic_html: true,
  aria_labels: 'complete',
  alt_text: 'descriptive, not decorative',

  // Audio/Video
  captions: 'required',
  transcripts: 'required',
  audio_descriptions: 'optional but encouraged',

  // Language
  lang_attribute: true,
  plain_language: true,
  reading_level: 'Grade 8 or below',

  // Cognitive
  clear_instructions: true,
  error_messages: 'specific and helpful',
  confirmation_dialogs: 'before destructive actions',

  // Elders
  larger_text_option: true,
  simpler_navigation: true,
  voice_commands: 'future enhancement'
}
```

---

## 🚀 Performance Targets

```typescript
const performanceTargets = {
  // Core Web Vitals
  lcp: '< 2.5s',  // Largest Contentful Paint
  fid: '< 100ms', // First Input Delay
  cls: '< 0.1',   // Cumulative Layout Shift

  // Images
  image_format: 'WebP with JPEG fallback',
  responsive_sizes: true,
  lazy_loading: true,

  // Fonts
  font_loading: 'swap',
  variable_fonts: true,  // Single file, multiple weights

  // JavaScript
  code_splitting: true,
  tree_shaking: true,
  bundle_size: '< 200KB initial',

  // Offline
  service_worker: true,
  offline_pages: true,
  cached_assets: true
}
```

---

## 📱 Mobile Considerations

### **Touch-Friendly**

- 48x48px minimum touch targets
- No hover states (they don't work on mobile)
- Swipe gestures for galleries
- Bottom navigation (thumb-friendly)

### **Bandwidth-Conscious**

- Progressive image loading
- Video thumbnails (not autoplay)
- Download size warnings
- Offline mode

### **One-Handed Operation**

- Important actions within thumb reach
- Bottom navigation bar
- Large tap targets
- No tiny text links

---

## 🎨 Cultural Design Elements

### **Indigenous Design Patterns**

```typescript
interface CulturalDesign {
  // Visual motifs from Palm Island
  patterns: {
    ocean_waves: 'SVG pattern for backgrounds',
    island_silhouette: 'Profile photo frame option',
    traditional_symbols: 'With proper permissions'
  },

  // Color symbolism
  colors: {
    ocean_blue: 'Sovereignty, connection to water',
    earth_green: 'Land, growth, healing',
    sun_yellow: 'Energy, life, new beginnings',
    fire_red: 'Strength, passion, cultural identity'
  },

  // Typography choices
  typography: {
    body: 'Clean, modern, readable',
    headings: 'Strong, bold, respectful',
    quotes: 'Serif, traditional, honored'
  },

  // Photography style
  photography: {
    professional: true,
    dignified: true,
    celebrates_community: true,
    avoids_poverty_porn: true,
    shows_strength_not_deficits: true
  }
}
```

---

This design system creates **world-class profile pages** that:

✅ Honor storytellers as knowledge keepers
✅ Accessible to elders and low-literacy users
✅ Audio-first for oral cultures
✅ Enforce cultural protocols
✅ Beautiful, professional quality
✅ Mobile-optimized for remote communities
✅ Privacy-first with granular controls

Next, I'll create the **Super Easy Import Workflows** document! 🚀
