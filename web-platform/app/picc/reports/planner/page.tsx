import { PlannerClient } from './PlannerClient'

export const metadata = {
  title: 'Report Content Planner — PICC',
  description: 'Visual content planner for annual report pages',
}

export default function ReportPlannerPage() {
  return <PlannerClient />
}
