export const metadata = {
  title: 'Annual Report Builder — PICC Admin',
  description: 'Generate audience-targeted annual reports from live PICC data.',
};

export default function ReportBuilderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
