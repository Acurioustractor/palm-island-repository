export const metadata = {
  title: 'Projects — PICC Admin',
  description: 'Internal projects view for PICC staff and leaders.',
};

export default function ProjectsAdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
