export const metadata = {
  title: 'Services — PICC Admin',
  description: 'Internal services view for PICC staff and leaders.',
};

export default function ServicesAdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
