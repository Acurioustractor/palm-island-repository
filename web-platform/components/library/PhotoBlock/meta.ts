import type { ComponentMeta } from '../registry'

export const meta: ComponentMeta = {
  name: 'PhotoBlock',
  description: 'Single photo with editorial italic caption (Fraunces 18, driftwood). Default 520px tall, configurable.',
  pencilFile: 'picc-almanac-web.pen',
  pencilNodeId: 'd5aQmE',
  category: 'almanac',
  implementations: ['web', 'pdf'],
  sortOrder: 50,
}
