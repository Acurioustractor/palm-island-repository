/** Pencil source: picc-almanac-web.pen → "05 · PhotoBlock" (d5aQmE). */
export interface PhotoBlockProps {
  /** Photo URL. */
  src: string
  /** Alt text for accessibility. */
  alt: string
  /** Caption (Fraunces 18 italic, driftwood). */
  caption?: string
  /** Photo height. Default 520px. */
  height?: number
}
