/** Pencil source: picc-almanac-web.pen → "08 · FooterCTA" (USpVp). */
export interface FooterCTAProps {
  /** Title in Fraunces 64 starGold. */
  title: string
  /** Body description on midnight bg. */
  body: string
  /** Optional CTA button label. */
  cta?: string
  /** Optional CTA href. */
  ctaHref?: string
}
