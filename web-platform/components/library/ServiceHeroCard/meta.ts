import type { ComponentMeta } from '../registry'

export const meta: ComponentMeta = {
  name: 'ServiceHeroCard',
  description:
    'Featured service spotlight — split layout with cover photo on the left and a shell-bg body on the right (eyebrow caps in section tint, Fraunces 42 ocean name, Inter description, optional Caveat fact strap). For landing pages, anchor stories, and /services/[slug] heroes.',
  pencilFile: 'picc-almanac-web.pen',
  pencilNodeId: 'C64BFX',
  category: 'almanac',
  implementations: ['web', 'pdf'],
  sortOrder: 30,
}
