import type { ComponentMeta } from '../registry'

export const meta: ComponentMeta = {
  name: 'WebHero',
  description:
    'Top-of-page hero — full-bleed image, gradient overlay, eyebrow caps, Fraunces 120 white title, optional Fraunces 36 starGold subtitle. Web only (PDF uses a separate cover treatment).',
  pencilFile: 'picc-almanac-web.pen',
  pencilNodeId: 'G8642',
  category: 'almanac',
  implementations: ['web'],
  sortOrder: 5,
}
